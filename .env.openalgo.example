# OpenAlgo Configuration
# =======================
#
# Copy this file to .env and fill in your actual values
# cp .env.openalgo.example .env
#
# OpenAlgo is a unified trading platform that supports multiple brokers.
# You need to have OpenAlgo server running and configured with your broker.
#
# OpenAlgo Documentation: https://docs.openalgo.in/

# ========================================================================
# OPENALGO API CREDENTIALS
# ========================================================================

# Your OpenAlgo API key (get this from OpenAlgo web interface)
# Required for authenticating with the OpenAlgo server
OPENALGO_API_KEY=your_api_key_here

# OpenAlgo server URL (default is localhost:5000)
# Change this if your OpenAlgo server is running on a different host/port
OPENALGO_HOST=http://127.0.0.1:5000

# ========================================================================
# BROKER CONFIGURATION (for downloading instruments)
# ========================================================================

# Broker name for downloading instrument master files
# Options: zerodha, fyers, fyrodha (paper trading)
# This is only used for downloading instrument lists
BROKER_NAME=zerodha

# ========================================================================
# OPTIONAL: BROKER-SPECIFIC CREDENTIALS (if needed for instruments download)
# ========================================================================

# These are only needed if you're using the BrokerGateway for downloading
# instrument files. Once downloaded, OpenAlgo handles all trading operations.

# For Zerodha (optional, only for downloading instruments)
# BROKER_API_KEY=your_zerodha_api_key
# BROKER_API_SECRET=your_zerodha_api_secret
# BROKER_ACCESS_TOKEN=your_zerodha_access_token

# For Fyers (optional, only for downloading instruments)
# BROKER_API_KEY=your_fyers_api_key
# BROKER_API_SECRET=your_fyers_api_secret
# BROKER_ACCESS_TOKEN=your_fyers_access_token

# ========================================================================
# LOGGING CONFIGURATION
# ========================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
